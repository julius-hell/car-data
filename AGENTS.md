## Agent skills

### Issue tracker

Issues live as GitHub Issues (`gh` CLI) in `julius-hell/car-data`. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
